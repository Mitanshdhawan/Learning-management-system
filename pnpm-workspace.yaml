packages:
  - "apps/*"
  - "packages/*"

# Approve build scripts for these trusted dependencies (pnpm 11 requires explicit opt-in).
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
  sharp: true
